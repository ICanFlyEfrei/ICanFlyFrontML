export interface PlaneCard {
    id: number;
    airline: string;
    departureDate: Date;
    arrivalDate: Date;
    departure: string;
    arrival: string;
    destination: string;
    price : number;
}